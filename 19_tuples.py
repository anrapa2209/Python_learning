#tuples are immutable, we cannot mutate/change them
numbers = (1, 2, 3)                       #use [] to define lists, () to define tuples
# numbers[0] = 10
print(numbers[0])
# numbers.count()                         #we cannot add, remove, change any items in a tuple
# numbers.index()