coordinates = (1, 2, 3)                     #(x, y, z)
#coordinates[0] * coordinates[1] * coordinates[2]        #code will be very long
# x = coordinates[0]
# y = coordinates[1]
# z = coordinates[2]
#instead of repeating coordinates[], we can simply use:x * y * z
x, y, z = coordinates
print(x)
print(y)
print(z)